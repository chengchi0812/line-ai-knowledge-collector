export default function Home() {
  return <main>
    <h1>LINE AI 個人知識庫</h1>
    <p>Webhook service is running.</p>
    <p>健康檢查：<code>/api/health</code></p>
    <p>LINE Webhook：<code>/api/line/webhook</code></p>
  </main>;
}
